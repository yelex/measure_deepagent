# Analyst review — 2026-08-13T15:02:12Z

## Контекст восстановлен из

`IMPROVEMENT_BACKLOG.md`, `data/output/tuning_log.jsonl` (все 5 записей),
`docs/Методика и критерии оценки качества извлечения данных-v10-*.docx`
(§1-5), `docs/Обработка НПА-v17-*.docx` (§2, спецификация полей),
`git log --oneline -30`, `RALPH_PROMPT_CODER.md`, `RALPH_PROMPT_TESTER.md`,
`ralph_loop.sh`, оба `data/output/ralph_iteration_*.md` отчёта Coder'а за
2026-08-12/13, а также прямой запуск `score_against_golden.py --no-log`
(без записи в tuning_log) на текущем экспорте и на экспорте коммита
`4547425` для проверки гипотез.

## Главная находка: необнаруженная регрессия (L011)

Код L011 (`051a2e2`, 2026-08-12 22:32) ни разу не прошёл Tester-проверку.
Два Coder-прохода подряд (22:34 и следующего дня 14:49) не довели
итерацию до записи `ralph_handoff.json` — файла нет в репозитории, значит
Tester по своему протоколу ни разу не запускался за 18+ часов. Прямая
проверка `agent_cards_export.json` (уже перегенерированного и
закоммиченного, `0a1c969`) показала: **21/21 карточек ЖС "инвалиды" и
14/16 карточек "сво" содержат только `measureId/region/measureName` —
без единого другого поля**. Повторный запуск `score_against_golden.py
--no-log` на этом экспорте против прогона на дорегрессионном экспорте
(`4547425`, тем же scorer'ом) подтвердил: AvgQS 0.351 → **0.241** (-31%
по всему датасету, -50% для "инвалиды"). Root cause: `has_relevant_content()`
(L011) применяется ко всем ЖС без ограничения доменом cntd.ru (как
задумывалось по названию задачи), ложно бракует короткие
однопредметные документы и форсирует лишние вызовы `search_and_fetch_npa`
(L008) — похоже на каскадное исчерпание квоты/рейт-лимита Yandex Search
API по ходу батч-прогона (порядок деградации точно совпадает с порядком
ЖС в `data/measures_registry.json`: вбд не пострадал → сво частично →
инвалиды полностью). Подробности и рекомендация — в `IMPROVEMENT_BACKLOG.md`,
запись L011 (переведена в `blocked`, приоритет critical, самая срочная
задача для следующего Coder-прохода).

## Тренд метрик

Валидные (не искажённые сменой scorer'а) точки под СТРОГИМ scorer'ом:
AvgQS 0.248 (L002) → 0.301 (L008) → 0.322 (L003) — устойчивый рост.
2026-08-12 21:50 человек осознанно вернул pymorphy3-лемматизацию в
scorer (`7a51564`, human-approved, задокументировано в
`RALPH_PROMPT.md`) — это смена шкалы измерения, не прогресс экстракции;
на новой шкале последняя ПОДТВЕРЖДЁННАЯ точка — AvgQS 0.371. С тех пор
tuning_log не пополнялся: видимая "стагнация" — на самом деле полное
отсутствие измерений на фоне регрессии (см. выше). Recall/Precision
стабильны на 0.86/1.00 во всех 5 записях — Precision уже выше цели
(≥0.85), Recall ниже цели (≥0.90) и структурно ограничен ЖС "вбд"
(Recall=0.5, см. L011). **PerfectRate = 0.0 во ВСЕХ 5 записях без
исключения** — ни одна из 43 карточек ни разу не набрала QS=1.0, даже
когда AvgQS рос. Это означает, что ошибки размазаны по многим
полям/карточкам, а не сконцентрированы — точечных "почти идеальных"
карточек, которые легко дотянуть до 1.0, обнаружить пока не удалось
(для этого нужен персональный отчёт по карточкам, которого scorer сейчас
не сохраняет за пределами stdout — соответствующий разрыв в
инструментарии стоит иметь в виду, но чинить `score_against_golden.py`
запрещено).

## Root-cause разбор (по методике §1.3, §3)

**L012 (sum-ошибки "инвалиды")** — разобраны все 21 карточка (не 3-4,
весь ЖС) на baseline-экспорте `4547425`, чтобы не путать с регрессией
L011. 16/21 агрегированных ошибок: 13 — полный промах (в основном
data-проблема реестра: 8 мер вообще без `amountsUrl`, ссылаются только
на процедурные mos.ru-страницы без суммы; ещё несколько мер имеют
`amountsUrl` на общий индексационный документ, но `_cut_to_relevant`,
похоже, промахивается мимо нужной строки в длинном перечне — тот же
класс проблемы, что чинился в L003 для основного источника, но не
распространён на второй источник); 2 — group-collapse (сумма найдена,
но не продублирована по всем 4 групповым полям там, где golden это
делает для не различающих группу мер); 1 — генуинно неверное значение;
5 — совпадают (после нормализации чисел в `sum_field_match` разница в
форматировании — не ошибка). План по каждой причине зафиксирован в
`IMPROVEMENT_BACKLOG.md` (L012, статус переведён в actionable/done
root-cause).

**Category-поля** — при попутной проверке оказались крупнейшим
единственным источником QS-ошибок сразу на всех 3 ЖС (вбд 6/6, сво
15/16, инвалиды 18/21 на baseline-прогоне) — крупнее, чем sum/terms/
department по отдельности в любой из ЖС. L010 (category static lookup
для сво/инвалиды) подтверждён как приоритет high, не понижать.

**L009 (department lookup)** — работает, но `_DEPARTMENT_EXCEPTIONS`
построен как словарь `measureName → golden-значение` для 7 конкретных
мер. Структурно это тот же паттерн memorization, от которого backlog
явно отказался в преамбуле ("regex-эра — hand-crafted парсер под
конкретный URL"). Не блокирует, но зафиксировано как риск на будущее
(masштабирование L005, соблазн применить тот же приём к другим полям) —
см. запись L009.

## Решение по L007 (swarm-эксперимент)

Не начинать. Текущая "стагнация" объясняется как минимум наполовину
необнаруженной регрессией (L011) и пробелами реестра (L012), а не
исчерпанием возможностей монолитного промпта — дешёвые направления
(L010, L011-фикс, L012-registry) не испробованы. Пересмотреть после
следующего цикла Analyst, если AvgQS не сдвинется даже после этих
фиксов.

## Изменения в IMPROVEMENT_BACKLOG.md

- L011: `todo` → `blocked`, приоритет critical подтверждён, добавлен
  количественно подтверждённый root-cause регрессии и рекомендация.
- L012: `todo` → root-cause done, добавлен конкретный actionable план,
  приоритет medium → high.
- L009: добавлено предостережение про memorization-риск в
  `_DEPARTMENT_EXCEPTIONS` (статус done не менялся).
- L010: приоритет high подтверждён (не понижать), добавлен скоуп-гард
  (не переносить на "вбд").
- L007: добавлено явное решение "не начинать сейчас" с обоснованием.
- Наверху файла: уточнение про `7a51564` (человек легитимно тронул
  scorer, это не отменяет правило для Coder/Tester/Analyst) и явный
  список приоритетов на ближайшую итерацию.

Working tree чист, изменения только в `IMPROVEMENT_BACKLOG.md` и этом
отчёте — код/scorer/golden/tuning_log/review_queue не тронуты (весь
запуск `score_against_golden.py` делался с `--no-log`, без записи в
tuning_log).
