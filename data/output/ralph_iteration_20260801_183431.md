# ralph_iteration_20260801_183431 — B003 (continued), десятый seed инвалиды (77_10)

## Задача
`IMPROVEMENT_BACKLOG.md` B003 (in_progress) — расширить покрытие ЖС
"инвалиды" ещё на один seed по тому же паттерну, что и предыдущие 9
итераций (77_1..77_9): найти рабочий источник для следующей непокрытой
меры эталона, написать `extract_disability_*_card`, добавить seed в
`data/measures_registry.json`, прогнать eval до/после.

Целевая мера: `77_10` — "Бесплатный проезд в наземном транспорте,
метрополитене, МЦК, МЦД, а также на железнодорожном транспорте в
пределах Москвы и области" (эталон: `measure_first/second/third_group` =
`measure_disabled_child` = "оформление социальной карты"; `measurePeriodicity`
= "единовременно"; `measureTerms` = "Факт установления инвалидности,
сопровождающим инвалидов. Наличие регистрации по месту жительства в
г. Москве"; `department` = "ГУП города Москвы «Московский социальный
регистр»"; cause_* все = 1). Эталонная `Ссылка на источник` указывает на
общий хаб `kak-poluchit-pomosch-dlya-invalidov` — по прецеденту 77_9 эта
страница не содержит нужного текста (проверено там же), поэтому сразу
искал альтернативу через SearXNG, не тратя цикл на подтверждение того же
факта повторно. Эталонный "Нормативно-правовой акт - NPA" —
`docs.cntd.ru/document/3662941?marker=7DE0K6` (тот же закон N60 "О
социальной поддержке семей с детьми", что уже фетчится для 77_1/3/4/6) —
проверил полнотекстовым поиском: "проезд" в этом законе не встречается
ни разу (0 совпадений), значит этот закон не годится как текстовый
источник для терминов/суммы этой конкретной строки (в отличие от 77_1
и др., где он реально называет меру) — не использую его в extractor'е,
хотя эталон на него ссылается (NPA URL полностью выведен за пределы
scoring — `LS_SPEC["инвалиды"]["columns"]` в scorer'е не включает NPA-
поля вообще, см. `scripts/score_against_golden.py`).

## Baseline (до изменения)
Из `data/output/tuning_log.jsonl`, последняя запись (после 77_9,
commit `ef3b452`):
- Recall = 0.220 (11/50)
- Precision = 1.000
- Average QS = 0.5985
- Perfect Rate = 0.000
- инвалиды: Match=9/22, Recall=0.409, AvgQS(инвалиды)=0.6667

## Гипотеза
SearXNG-поиск ("бесплатный проезд инвалиды социальная карта москвича
наземный транспорт метро") находит `mos.ru/karta-moskvicha/tipy-derzhataley/invalidy/`
— выделенная страница "Карта москвича для инвалидов 1, 2 и 3 группы":
подтверждает дословно категорию получателей ("Граждане с
инвалидностью, зарегистрированные в Москве") и льготу ("Бесплатный
проезд на общественном транспорте: Городской транспорт Москвы
(электробусы/автобусы/трамваи/метро/МЦК), Общественный транспорт
Московской области, Пригородный ж/д транспорт (электрички/МЦД),
Аэроэкспресс"). Если добавить `extract_disability_social_card_transport_card`
на основе этого источника (без второго НПА-источника — закон 3662941
не подходит по терминам, поэтому один источник, как в 77_9), то
Recall/Match инвалиды вырастет на 1, Average QS не должен упасть
(department останется `None` — страница не называет ГУП "Московский
социальный регистр" рядом со льготой; periodicity останется `None` —
страница описывает процесс оформления, но не называет периодичность
дословно "единовременно").

## Как проверять
```
pyenv exec python3 scripts/score_against_golden.py --agent-export data/output/agent_cards_export.json --note "B003: до добавления 77_10"
# внести изменение (pipeline.py + measures_registry.json)
pyenv exec python3 scripts/run_pipeline_demo.py
pyenv exec python3 scripts/score_against_golden.py --agent-export data/output/agent_cards_export.json --note "B003: после добавления 77_10"
```
Критерий commit: Recall/Match инвалиды выросли, Precision не упал (0
Error_agent/Miss_etalon), Average QS не упал относительно 0.5985.
