# Ralph iteration 20260801_181656

## Задача

`B003` (in_progress) — расширить покрытие ЖС "инвалиды" ещё на один seed.
Восьмая мера подряд в этой серии итераций, продолжение паттерна
77_1/77_5/77_2/77_3/77_4/77_6/77_8.

## Baseline (до изменения)

Прогон `pyenv exec python3 scripts/score_against_golden.py
--agent-export data/output/agent_cards_export.json --note "B003: до
добавления 77_7 (вознаграждение приёмному родителю/патронатному
воспитателю)"`:

- Recall общий: 0.180 (9/50)
- Precision: 1.000
- Average QS: 0.598 (на 9 карточках)
- Perfect Rate: 0.000
- инвалиды: Recall=0.318 (7/22), AvgQS=0.686

## Выбранная мера

`77_7` — "Вознаграждение приёмному родителю, патронатному воспитателю"
(эталон, лист "инвалиды"): `cause_disabled_child=1`,
`measure_disabled_child=39856`, `measurePeriodicity=ежемесячно`,
`measureTerms="На каждого ребёнка-инвалида, переданного на воспитание в
приёмную семью, на патронатное воспитание. Заявитель и ребёнок должны
иметь регистрацию по месту жительства в Москве"`, `department=Департамент
труда и социальной защиты населения Москвы`.

Эталонные ссылки: `Ссылка на источник` =
`mos.ru/otvet-socialnaya-podderjka/kak-poluchit-pomosch-dlya-invalidov/`,
`НПА` = `docs.cntd.ru/document/3662941`, `Ссылка на НПА` =
`docs.cntd.ru/document/1314770295`.

## Гипотеза

Ручная проверка (`fetch_text(..., use_proxy=True)` вживую, без записи в
код) двух источников, УЖЕ используемых в предыдущей итерации (77_8):

1. `mos.ru/otvet-socialnaya-podderjka/kak-poluchit-vyplaty-usynovitelyam-opekunam-priemnym-roditelyam/`
   (та же FAQ-страница, что и для 77_8, НЕ та ссылка, что указана в
   колонке эталона для 77_7 — эталонная ссылка ведёт на другую
   FAQ-страницу `kak-poluchit-pomosch-dlya-invalidov`, но методика
   (`skills`/AGENTS.md) не требует совпадения самого URL источника,
   только фактического подтверждения поля цитатой; эта уже проверенная
   страница содержит отдельный вопрос №7 "Как оформить ежемесячное
   вознаграждение приемным родителям?" с блоком `"id":1450,"title":"Как
   оформить ежемесячное вознаграждение приемным родителям?"`, ответ
   `"id":1657` даёт текст условия почти дословно совпадающий с эталонным
   `measureTerms` (регистрация в Москве и заявителя, и ребёнка).
2. `docs.cntd.ru/document/1314770295` (то же постановление №3025-ПП, уже
   используемое как `amountsUrl` для 77_1/2/3/4/5/6/8) — раздел §2.5
   "Ежемесячное вознаграждение приемному родителю (приемным родителям),
   патронатному воспитателю:", подпункт §2.5.2 "на каждого
   ребенка-инвалида, переданного на воспитание в приемную семью, на
   патронатное воспитание" → **39856** — дословное совпадение суммы и
   формулировки с `measure_disabled_child` эталона. §2.5.1 (не-инвалид)
   даёт другую сумму (23446), матчинг блока "2.5 ... до 2.6", чтобы не
   перепутать со следующим разделом.

Ожидаемый эффект: Match инвалиды 7→8, Recall общий 0.180→0.200,
Average QS должен вырасти (новая карточка ожидаемо получит QS≥0.5, как и
предыдущие 7). Precision/Perfect Rate не должны просесть (обе метрики
пока не участвуют в тюнинге по 0-факту: Precision=1.000 удерживается тем,
что extractor возвращает `measureId: None` при неподтверждённой сумме —
не пишет заведомо ложные карточки).

`department`: как и в 77_8 (тот же FAQ-источник), конкретный блок ответа
№7 не называет ведомство рядом с суммой/условием — оставляю `None`,
не угадываю по общей ссылке на "Департамент труда и социальной защиты"
(она относится к процедуре подачи справки, не именно к этой выплате).
Ожидаемо `department` не совпадёт со scorer'ом на этой карточке — цена
честности выше угадывания.

## Как проверяю

```bash
pyenv exec python3 scripts/score_against_golden.py --agent-export data/output/agent_cards_export.json --note "B003: после добавления 77_7"
```

Сравниваю Recall/Precision/AvgQS/Perfect Rate с baseline выше. Ожидаю
Recall 0.180→0.200, Precision/Perfect Rate без изменений, AvgQS рост.
