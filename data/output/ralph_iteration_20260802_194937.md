# ralph_iteration_20260802_194937 — B003 (continued), тридцать второй seed сво

## Задача

`B003` (`IMPROVEMENT_BACKLOG.md`) — расширение минимального пайплайна на
ЖС сво/инвалиды. Статус: `in_progress`. Остаётся 4 непокрытые меры сво
(77_svo_2, 77_svo_3, 77_svo_16, 77_svo_18) + 2 инвалиды (77_18, 77_22,
дважды заблокированы на источнике текста — не трогаю в этой итерации).

## Baseline (до изменения)

Из `tuning_log.jsonl` (запись `"B003: до добавления 77_svo_2 (ralph
pre-check)"`, тот же снимок, что и предыдущий "после" 77_svo_1):

- Match=33, Recall=0.660 (33/50), Precision=1.000
- Average QS=0.5359, Perfect Rate=0.0303
- сво: Match=12/16, Recall=0.750
- инвалиды: Match=20/22, Recall=0.909 (без изменений — не трогаю)
- вбд: Match=1/12 (без изменений)

## Гипотеза

Прошлая итерация (`iteration_20260802_193658`, коммит `a6ee478`) оставила
заметку: "77_svo_2, 77_svo_3, 77_svo_16 — та же памятка
`disk.yandex.ru/d/V_LEIMTttBQTeQ` уже содержит их сумму/НПА дословно,
реализуются без повторной разведки". Перед тем как писать код, я заново
зафетчил тот же PDF (`fetch_yandex_disk_pdf_text`, 17405 симв.) и проверил
это утверждение полнотекстовым поиском:

- "50 000" / "ЕЖЕМЕСЯЧН" → **найдено** (раздел "ЕЖЕМЕСЯЧНЫЕ ВЫПЛАТЫ",
  "Ежемесячная денежная выплата от Мэра Москвы... Указ Мэра Москвы от
  22.09.2022 № 52-УМ 50 000,00") — подтверждает `77_svo_2` ("Ежемесячная
  выплата", golden сумма 50000₽/ежемесячно) практически дословно.
- "газифи" → **найдено** (раздел "ПОДКЛЮЧЕНИЕ К СЕТЯМ ГАЗОСНАБЖЕНИЯ",
  "компенсационная выплата за газификацию не более: •покупка
  оборудования - 114 000,00 руб. •подключение... - 84 000,00 руб.") —
  подтверждает `77_svo_16` дословно на сумму 114000.
- "ранени" → **НЕ найдено ни разу** — заметка предыдущей итерации была
  неточной для `77_svo_3` ("Выплата в случае ранения", 500000₽+): этот
  конкретный PDF не описывает выплаты за ранение вообще. Проверять
  заново перед реализацией, не доверять заметке вслепую (сама заметка
  теперь исправлена в PROGRAM.md следующей записью).

Гипотеза (одно изменение за итерацию, беру `77_svo_2` как наиболее
дословно подтверждённый источник): добавить
`extract_svo_monthly_payment_card` + ветку диспатча по `measureName` в
`run_svo_seed` для источника `disk.yandex.ru/d/V_LEIMTttBQTeQ` (сейчас
там безусловно вызывается `extract_svo_contract_payment_card` для любой
меры с этим `npaUrl` — нужно дискриминировать по имени меры, как уже
сделано для cntd-ветки). Ожидаемый эффект: Match сво 12→13, Recall общий
0.660→0.680 (34/50), Precision не должна измениться (1.000).

`77_svo_16` и `77_svo_3` оставляю на следующие итерации (одно изменение
за итерацию) — 77_svo_16 сейчас же, следующим шагом, разведка уже готова;
77_svo_3 требует нового источника (этот PDF не подходит).

## Как проверять

```
pyenv exec python3 scripts/run_pipeline_demo.py
pyenv exec python3 scripts/score_against_golden.py --agent-export data/output/agent_cards_export.json --note "..."
```

Сравнить Match/Recall/Precision/AvgQS/PerfectRate до/после, проверить что
field-errors по остальным 32 карточкам не изменились (регрессии нет).
