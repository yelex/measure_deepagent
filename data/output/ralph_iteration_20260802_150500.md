# Ralph iteration 20260802_150500 — B003 (continued), тридцать первый seed сво (77_svo_18)

## Задача
`IMPROVEMENT_BACKLOG.md` [B003] — расширить минимальный пайплайн на сво и
инвалиды. Статус: `in_progress`. Один seed за итерацию.

## Baseline (до изменения)
Из `data/output/tuning_log.jsonl`, последняя запись (git_commit `cc691ba`,
после 77_svo_21 — commit `4605468`):
- Match = 32/50, Recall = 0.640, Precision = 1.000, Average QS = 0.5276,
  Perfect Rate ≈ 0.031
- сво: 11/16 покрыто. Остаются: 77_svo_1, 2, 3, 16, 18.
- инвалиды: 20/22 покрыто. Остаются 77_18/77_22 — дважды заблокированы на
  источнике текста (см. заметки в backlog), не берём в этой итерации.

## Гипотеза
Backlog (`IMPROVEMENT_BACKLOG.md`, конец истории по iteration_20260802_124851)
прямо называет следующий кандидат без повторной разведки: `77_svo_18`
("Карьерное консультирование, профориентация") ↔ документ
`cntd.ru/document/1300860766`, п.1.11 — таблица сопоставления сверена
целиком в iteration_20260802_121632 и подтверждена без расхождений в
четырёх последующих итерациях.

Проверено прямым повторным фетчем документа перед правкой кода (см. ниже):
п.1.11 = "Содействие в поиске работы членам семьи." — короче и не дословно
совпадает с golden `measureName`/`measureTerms` (golden описывает шире:
"профориентация, карьерное консультирование, поиск работы, подготовка к
трудоустройству"), но по существу — та же мера (содействие в
трудоустройстве). Golden для этой строки: `categoryMobilized=1`,
`categoryContractor=1`, `categoryVolunteer=1`, `kidsOfMilitary=0`,
`department`="Единый центр поддержки участников СВО и их семей" (в тексте
указа не встречается — как и во всех предыдущих сво-карточках).

По аналогии с `extract_svo_legal_aid_card` (последний добавленный extractor)
добавить `extract_svo_career_counseling_card` + двенадцатую ветку диспатча
в `run_svo_seed` по `measureName`, plus seed в `data/measures_registry.json`.

Ожидаемый эффект: Match 32→33, Recall 0.640→0.660, Precision не должна
измениться (1.000), Average QS — композиционный эффект знаменателя (новая
карточка вероятно ниже среднего, т.к. terms/department снова не
подтвердятся дословно — системное ограничение источника, установленное во
всех предыдущих 11 сво-seed'ах), не регрессия.

## Как проверять
```bash
pyenv exec python3 scripts/score_against_golden.py --agent-export data/output/agent_cards_export.json --note "B003: до добавления 77_svo_18 (ralph pre-check)"
# ... изменение ...
pyenv exec python3 scripts/run_pipeline_demo.py  # (или актуальный способ прогона сво-seed'ов)
pyenv exec python3 scripts/score_against_golden.py --agent-export data/output/agent_cards_export.json --note "B003: после добавления 77_svo_18 (карьерное консультирование, cntd.ru/1300860766 п.1.11)"
```
Сравнить overall.recall/precision/average_qs/perfect_rate до/после,
field_errors по 32 существующим карточкам не должны измениться (regression
check).
