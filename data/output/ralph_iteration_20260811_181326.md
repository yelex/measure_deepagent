# ralph_iteration_20260811_181326 — B003 (continued), тридцать третий seed сво

## Задача

`B003` (`IMPROVEMENT_BACKLOG.md`) — расширение минимального пайплайна на
ЖС сво/инвалиды. Статус: `in_progress`. По заметке предыдущей итерации
(`iteration_20260802_194937`, коммит `0e9bf30`): "77_svo_16 сейчас же,
следующим шагом, разведка уже готова" — та же памятка на Яндекс.Диске
(`disk.yandex.ru/d/V_LEIMTttBQTeQ`), раздел "ПОДКЛЮЧЕНИЕ К СЕТЯМ
ГАЗОСНАБЖЕНИЯ", сумма 114000/84000 уже подтверждена дословно в прошлой
разведке.

## Baseline (до изменения)

Из `tuning_log.jsonl` (последняя запись, `2026-08-02T19:52:34`, "после
добавления 77_svo_2"):

- Match=34, Recall=0.680 (34/50), Precision=1.000
- Average QS=0.5377, Perfect Rate=0.0294
- сво: Match=13/16, Recall=0.8125
- инвалиды: Match=20/22, Recall=0.909 (без изменений — не трогаю)
- вбд: Match=1/12 (без изменений)

Прогнан `pre-check` в начале этой сессии (2026-08-11) — метрики те же
(код не менялся с 2026-08-02 в части export/pipeline для этих карточек,
только добавлен `llm_extract.py`, который никуда не подключён и не
затрагивает существующие карточки).

## Гипотеза

Добавить `extract_svo_gasification_compensation_card` +
дискриминацию по `measureName` (`startswith("Компенсационная выплата за
газификацию")`) в disk.yandex.ru-ветку `run_svo_seed`. Источник — тот же
PDF, перепроверен прямым повторным фетчем в этой сессии (**без прокси** —
в этой Linux-песочнице `cloud-api.yandex.net` отвечает напрямую 200, в
отличие от прошлых Mac-сессий, где был нужен `RU_PROXY_URL`; `cntd.ru`
direct fetch, наоборот, таймаутит — придётся использовать `use_proxy=True`
для cntd-веток, как раньше):

```
ПОДКЛЮЧЕНИЕ К СЕТЯМ ГАЗОСНАБЖЕНИЯ
компенсационная выплата за газификацию не более:
•покупка оборудования - 114 000,00 руб.
•подключение к сетям газоснабжения жилого дома в пределах г. Москвы - 84 000,00 руб.
❖ Условие: одному члену семьи
Постановление Правительства Москвы от 14.11.2023 № 2202-ПП
```

Golden (`77_svo_16`): measureSum="до 114 000 ₽ на покупку оборудования",
measurePeriodicity="единовременно", measureTerms — развёрнутый текст про
"не более 84 000 ₽" и "36 месяцев" (**этого текста нет в PDF-памятке** —
памятка даёт только "Условие: одному члену семьи", более подробные
условия видны только в самом НПА 2202-ПП, не в этой памятке), department
golden="Социальное казначейство города Москвы" (**в PDF нет этой строки
вообще** — есть только общий футер памятки "Единый центр поддержки
участников СВО и членов их семей", как во всех предыдущих карточках с
этим источником). category*=1/1/1/0 в golden — раздел памятки не называет
конкретную подкатегорию (не "мобилизован"/"контрактник"/"доброволец"),
общий для всех разделов памятки, поэтому беру честно то же, что и в
77_svo_1/2 (categoryContractor=1 по общему заголовку памятки "ДЕЙСТВУЮЩИЙ
ВОЕННОСЛУЖАЩИЙ: ЗАКЛЮЧИЛ КОНТРАКТ", Mobilized/Volunteer=0 — текст их не
упоминает).

Ожидаемый эффект: Match сво 13→14, Recall общий 0.680→0.700 (35/50),
Precision не должна измениться (1.000). department/terms по этой карточке,
скорее всего, останутся полевыми ошибками (как у большинства предыдущих
сво-карточек) — это не регрессия, честный пробел источника.

## Как проверять

```
python3 scripts/run_pipeline_demo.py
python3 scripts/score_against_golden.py --agent-export data/output/agent_cards_export.json --note "B003: после добавления 77_svo_16 (компенсация за газификацию)"
```

Сравнить Match/Recall/Precision/AvgQS/PerfectRate до/после, проверить, что
field-errors по остальным 34 карточкам не изменились (регрессии нет).
