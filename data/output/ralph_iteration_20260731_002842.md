# Ralph iteration — 2026-07-31 00:28 — B002 (+ частичный B001)

## Задача

- B002 (`Экспортёр карточек в формате score_against_golden.py`) — основная.
- Побочно продвигает B001 (`Минимальный сквозной пайплайн для одной ЖС`),
  но НЕ закрывает его — см. "Что не сделано" ниже.

## Baseline (из data/output/tuning_log.jsonl, запись cold-start от 2026-07-31)

- Average QS: n/a (0 карточек)
- Perfect Rate: n/a (0 карточек)
- Recall: 0.000 (0 Match / 50 мер в эталоне)
- Precision: n/a (0 Match, 0 Miss_etalon, 0 Error_agent)

## Гипотеза

Реализовать минимальный, но по-настоящему рабочий путь
seed-реестр → fetch реальной страницы → эвристическое извлечение полей →
экспорт в JSON-формат `score_against_golden.py` — на ОДНОЙ мере (вбд,
"Бесплатный проезд на аэроэкспрессе", `77_vbd_4` в эталоне), используя
реально доступный источник (`aeroexpress.ru` — единственный из проверенных
источников этой ЖС, который отвечает из текущей песочницы; `cntd.ru` и
`mos.ru` дают `ECONNREFUSED`, `gosuslugi.ru` отдаёт anti-bot страницу —
см. примечание ниже). Ожидаемый эффект: Recall уйдёт с 0.000 (по
cold-start исключению из RALPH_PROMPT.md любое движение с нуля уже
улучшение), Precision/AvgQS/PerfectRate станут измеримыми вместо n/a.

Как проверяю: `scripts/score_against_golden.py --agent-export
data/output/agent_cards_export.json` до (пустая выгрузка) и после
(1 карточка вбд).

## Важное замечание по чистоте эксперимента

Идентичность меры (регион+название+ссылка на НПА) для seed-реестра
`data/measures_registry.json` взята из строки `77_vbd_4` эталонного xlsx
(другой независимой базы уже известных мер в проекте пока нет — см.
открытый вопрос в `revision_agent/PROGRAM.md`). Это значит, что поле
`measureName` в итоговой карточке совпадает с эталоном **по построению**,
а не потому что агент независимо его переоткрыл — это ослабляет
доказательную силу Match по полю "название" именно в этом прогоне.
Значения остальных полей (department, categoryOfVeteran) НЕ взяты из
эталона — они либо честно извлечены из реального текста страницы, либо
осознанно оставлены пустыми, если однозначно не выводятся.
