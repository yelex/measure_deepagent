# Ralph iteration 2026-08-02 15:01:42

## Задача
`B003` (in_progress, medium priority) — расширить минимальный пайплайн на
сво/инвалиды. Двадцать девятый сво-seed: `77_svo_19` ("Оказание
психологической помощи"), документ `docs.cntd.ru/document/1300860766`,
п.1.12 указа — следующий непокрытый пункт по таблице сопоставления,
целиком сверенной в `iteration_20260802_121632` и переиспользованной в
трёх последующих итерациях (77_svo_12, 77_svo_14, 77_svo_17) без
расхождений.

## Почему не B004/B001 (высокий приоритет, но todo)
B004 (todo, high) — три полноценных попытки поиска в рамках одной
итерации (`iteration_3`, 2026-07-31) уже уперлись в отсутствие
конкретного НПА с региональной льготой по транспортному налогу
(SearXNG нашёл только федеральную норму НК РФ, которая по условию не
подходит категориям) — правило "не выдумывать решение вслепую" не даёт
четвёртой попытки без новой идеи источника, а её в текущем контексте
нет. B003, наоборот, имеет чистый, многократно проверенный источник
(1300860766) с оставшимися непокрытыми пунктами и установленный паттерн
dispatch — ниже риск, выше вероятность измеримого движения recall за
одну итерацию, что и было критерием предыдущих 20+ итераций подряд.

## Baseline (до изменения)
Из `tuning_log.jsonl`, последняя запись перед этой итерацией
(commit `576f185`, note "после добавления 77_svo_17"):
- Match=30, Recall=0.600, Precision=1.000, AvgQS=0.5461, PerfectRate=0.0333
- по ЖС сво: Match было 9/16 (после 77_svo_17)

## Гипотеза
Добавление `extract_svo_psychological_help_card` (десятая ветка диспатча
в `run_svo_seed`) для `77_svo_19` поднимет Match сво 9→10, общий
Match 30→31, Recall общий 0.600→0.620. Precision не должна измениться
(=1.000, извлечение только из подтверждённого текста, honest None/0 для
неподтверждённых полей). Average QS может как вырасти, так и снизиться
композиционно (зависит от QS новой карточки против текущего среднего) —
это не признак регрессии, если field-errors по всем 30 существующим
карточкам не изменились.

Прямой повторный фетч документа 1300860766 ДО правки кода подтвердил:
"1.12. Оказание психологической помощи семье." — почти дословное
совпадение с golden `measureName` ("Оказание психологической помощи").
Golden категориальные поля для 77_svo_19: categoryMobilized=1,
categoryContractor=1, categoryVolunteer=1, kidsOfMilitary=1 (единственная
из всей серии, где ВСЕ четыре флага=1 в эталоне) — но преамбула указа
по-прежнему не упоминает "мобилизован" ни разу (системное ограничение
источника, задокументированное в iteration_20260802_145212), поэтому
`categoryMobilized` останется честно 0, как и во всех предыдущих
сво-карточках — это ожидаемое, не новое расхождение.

## Как проверяю
```bash
pyenv exec python3 scripts/score_against_golden.py --agent-export data/output/agent_cards_export.json --note "B003: до добавления 77_svo_19"
# правка кода + прогон экспортёра
pyenv exec python3 scripts/score_against_golden.py --agent-export data/output/agent_cards_export.json --note "B003: после добавления 77_svo_19"
```
Критерий commit: Recall не падает (должен вырасти), Precision не падает
(=1.000), field-errors по существующим 30 карточкам не регрессируют.
