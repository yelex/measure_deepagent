# ralph_iteration_20260801_173818 — B003 (continued), третий seed "инвалиды" (77_2)

## Задача
`IMPROVEMENT_BACKLOG.md` → `B003` (in_progress, приоритет medium) —
расширение минимального пайплайна на ЖС инвалиды. Покрыто 2/22 мер
(`77_1`, `77_5`). Предыдущая итерация (`iteration_20260801_173231`) явно
оставила breadcrumb: таблица постановления 1314770295 §1.3 содержит ещё
п.1.3.2 ("неработающим родителям, являющимся инвалидами I или II группы"),
и для него уже найден mos.ru-лендинг
(`landing/target/7700000000163131356`) — следующая итерация может
повторить паттерн `77_5` без нового поиска источника.

Проверил эталон напрямую (`docs/меры_автоагент_2.xlsx`, лист "инвалиды",
`measureId=77_2`) через `pyenv exec python3 + openpyxl` (Read-only,
скрипт/файл не редактировались):

- measureName: "Компенсация на ребёнка неработающих родителей — инвалидов
  I или II группы"
- cause_general_disease=1, cause_war_trauma=1, cause_radiation=1,
  cause_disabled_child=**None** (в отличие от 77_1/77_5, где все 4
  причины были 1 — здесь мера НЕ распространяется на случай "ребёнок
  сам инвалид с детства", т.к. мера предназначена родителям-инвалидам,
  а не ребёнку-инвалиду)
- measure_first_group=17790, measure_second_group=17790,
  measure_third_group=**None**, measure_disabled_child=**None** (мера
  только для родителей I/II группы инвалидности, не III и не
  "инвалид с детства")
- department: "Департамент труда и социальной защиты населения Москвы"
- НПА: docs.cntd.ru/document/3662941 (тот же базовый закон, что и 77_1) +
  docs.cntd.ru/document/1314770295 (то же постановление о суммах, что и
  77_1/77_5, ожидается п.1.3.2)

**Важно**: в отличие от 77_1/77_5, где булевы/суммовые поля были
структурно однородны (1/1/1/1, одна сумма на все 4 подполя — постановление
не разбивало строку по причине/группе), здесь эталон явно НЕ однородный
(3 из 4 cause=1, 2 из 4 group-сумм заполнены). Значит подход "если сумма
подтверждена — заполнить все 4 подполя одинаково" (как в
`extract_disability_care_compensation_card`/`extract_disability_lost_breadwinner_card`)
здесь заведомо даст неверный результат на 2 полях (cause_disabled_child,
measure_third_group, measure_disabled_child), если бездумно скопировать
паттерн. Экстрактор должен извлекать это разграничение из текста
источника (mos.ru лендинг обычно явно называет "родители — инвалиды I
или II группы", "ребёнок-инвалид" НЕ упоминается) — не копировать
структуру предыдущих двух seed'ов вслепую.

## Baseline (до изменения)
Прогон `pyenv exec python3 scripts/score_against_golden.py --agent-export
data/output/agent_cards_export.json --note "B003: до добавления 77_2"`:

- Recall: 0.080 (4/50)
- Precision: 1.000
- Average QS: 0.496 (на 4 карточках)
- Perfect Rate: 0.000
- По ЖС инвалиды: Recall=0.091 (2/22), AvgQS=0.700, scored=2

## Гипотеза
Зафетчить mos.ru-лендинг `landing/target/7700000000163131356` (найден в
предыдущей итерации, ещё не проверялся) + то же постановление
1314770295 через прокси, извлечь `department`/`terms` из встроенного
JSON (тот же паттерн regex, что и в `extract_disability_lost_breadwinner_card`
— `"organization":"..."` / `"comments":"..."`), подтвердить сумму 17790
рядом с текстом про "родителей — инвалидов I/II группы" в п.1.3.2
постановления. КЛЮЧЕВОЕ отличие от предыдущих двух seed'ов: causal/group
поля заполнять НЕ единообразно, а по факту того, что реально
подтверждается текстом источника (родители I/II группы — да;
III группа/ребёнок-инвалид с детства — нет, честно None/0, если текст
их не называет). Ожидаемый эффект: Match инвалиды 2→3, Recall общий
0.080→0.100, Average QS должен вырасти (структурно корректная, не
угаданная карточка).

## Как проверяю
`pyenv exec python3 scripts/score_against_golden.py --agent-export
data/output/agent_cards_export.json --note "B003: после добавления 77_2"`,
сравнение с baseline выше. Требование: Recall не должен упасть, Precision
должен остаться 1.000 (не добавлять карточку, если сумма/название не
подтверждены независимо, как в предыдущих seed'ах).
