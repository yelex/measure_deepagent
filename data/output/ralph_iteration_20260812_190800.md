# Ralph iteration 2026-08-12 19:08

## Задача
L008 (backlog: critical, todo) — поиск НПА через Yandex Search API +
загрузка полного текста, когда `fetch_source(seed.npaUrl)` отдаёт слишком
мало текста (cntd.ru оголовок без JS-рендеринга) или URL неизвестен.

## Как выбрана
Наивысший приоритет среди `todo` в backlog — L008 (critical), L001/L002
уже `done`. Обнаружено расхождение: L006 (medium) код-фикс закоммичен
(e56ac56, boolean_fields map) и подтверждён в текущем
`revision_agent/llm_extract_v2.py`, но backlog-статус не обновлён на
`done` и eval не прогнан — итерация 3 "умерла" по словам commit message.
Последняя итерация (323c3d9) частично начала L008 (текст в backlog +
`.env`-loading фикс в `run_pipeline_demo.py`), но не создала
`npa_fetcher.py` и не прогнала eval (нет новой записи в tuning_log).
Продолжаю именно L008 (highest priority todo), а не доделываю
задним числом чужую L006-eval — RALPH_PROMPT требует ровно одну задачу
за итерацию, L008 самая приоритетная непочатая.

## Baseline
tuning_log последняя запись: L002 (a647c99) — Recall=0.860
Precision=1.000 AvgQS=0.248 PerfectRate=0.000, 43 карточки.
Обратите внимание: текущий working tree содержит незакоммиченный (нет,
он закоммичен в 323c3d9) `.env`-loading фикс — не переоцененный вместе
с L006 boolean-fields фиксом. Baseline для сравнения — 43 карточки/43
seed'а, 7 miss_agent (ошибки загрузки, mos.ru).

## Гипотеза
`search_and_fetch_npa(measure_name, ls, region)` через `revision_agent
.npa_search.search_npa` (уже существует, ключи в `.env` подтверждены
рабочими — протестирован вручную, вернул 10 релевантных доменов) +
ранжирование по домену (pravo.gov.ru > cntd.ru > consultant.ru >
garant.ru > mos.ru, PDF выше HTML) + загрузка через существующий
`agent.pipeline_mode.fetch_source`-стратегию (direct/jina/proxy) для
HTML и отдельный PDF-путь для .pdf-ссылок — даст полный текст НПА для
seed'ов, где `fetch_source(npaUrl)` возвращает <2000 символов.

Ручной тест подтвердил: `search_npa` возвращает cntd.ru/mos.ru/pravo.gov.ru
результаты для "Бесплатный проезд..."; `fetch_text(cntd_url, use_proxy=True)`
даёт 19588 симв. (не только оголовок); прямой download mos.ru PDF
таймаутит (известная ГОСТ-TLS проблема, см. PROGRAM.md) — ранжирование
должно ставить mos.ru последним, чтобы срабатывал фоллбэк на
cntd.ru/pravo.gov.ru.

## План проверки
1. Создать `revision_agent/npa_fetcher.py`:
   `search_and_fetch_npa(measure_name, ls, region) -> str`.
2. Интегрировать в `run_pipeline_demo.py::run_llm_mode`: если
   `fetch_source(url)` даёт <2000 символов или падает — fallback на
   `search_and_fetch_npa`.
3. Ручной тест на seed `77_vbd_3` ("Бесплатный проезд на всех видах
   городского пассажирского транспорта") — текст должен содержать
   "проезд", не только заголовок.
4. `python3 scripts/run_pipeline_demo.py --llm-mode` — полный batch.
5. `python3 scripts/score_against_golden.py --agent-export ... --note
   "L008: NPA search fallback"`.
6. Критерий успеха (backlog): Recall/AvgQS не хуже L002 baseline
   (0.860/0.248), в идеале miss_agent (7) снижается за счёт
   успешного fallback-поиска НПА.
