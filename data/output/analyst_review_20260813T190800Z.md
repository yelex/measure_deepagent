# Analyst review — 2026-08-13T19:08:00Z (третий проход того же дня)

## Контекст восстановлен из

`IMPROVEMENT_BACKLOG.md` (целиком), `data/output/tuning_log.jsonl` (все
7 записей), `git log --oneline -30`, `data/output/ralph_handoff.json`
(последний, L013 confirm), `data/measures_registry.json` +
`docs/меры_автоагент_2.xlsx` (прямой подсчёт строк по всем 3 листам),
`data/output/agent_cards_export.json` (текущий, 2026-08-13 19:03).
Методика (`docs/Методика...docx` §3, целевые MVP-значения
Recall≥0.90/Precision≥0.85/AvgQS≥0.90/PerfectRate≥0.70) и предыдущие
2 отчёта от этого же дня уже зафиксировали контекст L011/L013 —
не пересказываю повторно, только дельта.

## Главная новая находка: Recall=0.86 — точный потолок покрытия реестра

Recall не сдвинулся ни разу ни в одной из 7 записей `tuning_log.jsonl`
с самого первого LLM-прогона (`3102eb4`, 2026-08-11) до последнего
подтверждённого Tester'ом (`b88dd8d`, 2026-08-13) — всегда `0.86`,
несмотря на минимум 6 фиксов экстракции за это время. Прямой подсчёт
строк golden (`docs/меры_автоагент_2.xlsx`) против seed'ов
(`data/measures_registry.json`) даёт: вбд 12 golden / 6 registry (не
хватает `77_vbd_11-16`), сво 16/16 (полное покрытие), инвалиды 22/21 (не
хватает одной меры). Итого 43/50 = **0.86** — совпадает с наблюдаемым
Recall до последнего знака. Вывод: Recall физически не может подняться
выше 0.86, пока эти 7 мер (в основном вбд) не появятся в реестре —
никакой prompt-tuning, lookup-словарь или архитектурный эксперимент
(L007) этого не изменит. Заведено как эскалация [L005]
(`low`→`critical`) с конкретным списком недостающих `measureId`.

Побочно проверено и задокументировано: domain-scope фикс L011
(`7baa898`), который целился именно в вбд, эмпирически НЕ изменил AvgQS
вбд ни на бит (идентичное значение `0.3111...` во всех записях с
`4547425`, т.е. ДО того как регрессия вообще была внесена, и до сих
пор). Весь наблюдаемый рост AvgQS 0.371→0.499 объясняется
восстановлением сво/инвалиды после GLM-квота-каскада (L013), а не
domain-scope фиксом. Закрыл L011 как `done` (заявленный симптом —
пустые вбд-карточки — не воспроизводится в текущем экспорте), но с
явной оговоркой, что его оригинальная гипотеза причины не
подтвердилась — реальный виновник Recall=0.5 для вбд всегда был
отсутствием seed'ов, не качеством контента.

## Тренд метрик (обновлён с учётом 2 новых записей tuning_log)

AvgQS: 0.248 (L002) → 0.301 (L008) → 0.322 (L003) → [0.371 — смена
шкалы scorer'а, не прогресс] → **0.499 (L013 confirm, `b88dd8d`)** —
здоровый рост после разрешения GLM-квота-катастрофы. Recall/Precision:
0.86/1.00 стабильно все 7 записей — Precision выше цели методики,
Recall ниже цели и теперь имеет задокументированную структурную причину
(см. выше), а не "неизвестную стагнацию". PerfectRate=0.0 все 7 записей
— ожидаемо, ни L010 (category lookup), ни L012-action (sum) ещё не
внедрены.

## Root-cause разборы

Новый разбор в этом проходе — структурный (реестр vs golden), не
точечный по карточкам (L012 уже имеет детальный per-card root-cause из
предыдущего прохода, без изменений). L009 (department
memorization-риск) без изменений.

## Решение по L007 (swarm-эксперимент)

Без изменений: держать в `todo`. Причина стагнации была "нет валидных
измерений" — снята (L011/L013 подтверждены). Но три более дешёвых
рычага (L005-critical, L010-high, L012-action) ещё не испробованы;
swarm не решает ни один из них напрямую (архитектура экстрактора не
чинит отсутствующие seed'ы или lookup-словари).

## Отдельная находка: процессная проблема (async eval) похоже устранена технически

Коммит `6ad8aae` перенёс синхронный eval из-под Coder-агента в скрипт,
который сам пишет `ralph_handoff.json` — технический guard вместо
очередной правки текста промпта. С этого коммита 2 цикла Coder→Tester
прошли штатно (`891b5a5`, `7e22101`), оба раза handoff создан и Tester
реально сработал. Рано закрывать пункт насовсем (маленькая выборка),
следующий Analyst-проход должен перепроверить на большем числе циклов.

## Изменения в IMPROVEMENT_BACKLOG.md

- **[L005]** эскалирован `low`→`critical`, добавлена находка (Recall
  0.86 = точное покрытие реестра 43/50) с конкретным списком
  недостающих `measureId` (`77_vbd_11-16`, один инвалиды) и action-планом.
- **[L011]** статус `in_progress`→`done`, с оговоркой про
  неподтверждённый эффект domain-scope фикса на вбд AvgQS конкретно и
  ссылкой на [L005] как настоящую причину Recall=0.5.
- **[L007]** решение "не начинать" переподтверждено с обновлённым
  обоснованием (дешёвые рычаги: L005/L010/L012, не отсутствие данных).
- Обновлена преамбула ("Ближайший приоритет") — актуальный порядок:
  L005 (critical) → L010 (high) → L012 (action, high).
- Добавлена заметка про вероятное устранение async-eval протокольного
  сбоя (коммит `6ad8aae`), с пометкой "рано считать окончательно
  закрытым".

Working tree чист до коммита; изменения только в
`IMPROVEMENT_BACKLOG.md` и этом отчёте — код/scorer/golden/tuning_log/
review_queue не тронуты, ничего не запускалось (весь анализ — чтение
существующих файлов + прямой подсчёт строк реестра/golden в Python).
