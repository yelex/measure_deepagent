# Ralph iteration 20260802_120844

## Задача

`[B003]` Расширить минимальный пайплайн на сво и инвалиды — двадцать
четвёртый seed сво: `77_svo_8` ("Перевод в детский сад или школу рядом с
домом").

## Baseline (перед изменением)

Из последнего коммита (`e158e32`, 77_svo_7): Match 25/50, Recall 0.500,
Precision 1.000, Average QS 0.605, Perfect Rate 0.040. Покрытие сво: 4/16
(77_svo_6, 77_svo_7, 77_svo_9, 77_svo_10). Инвалиды: 20/22 (77_18/77_22
заблокированы на источнике текста, см. заметки в backlog). Вбд: 1/12.

## Гипотеза

Тот же документ `docs.cntd.ru/document/1300860766` (уже фетчащийся для
4 других сво-мер), но п.1.2 (а не п.1.1/1.3/1.4/1.5, уже занятые другими
мерами). Golden сверен напрямую (`docs/меры_автоагент_2.xlsx`, лист
"сво", `measureId=77_svo_8`):
- `measureName` = "Перевод в детский сад или школу рядом с домом"
- `measureTerms` (golden) = "Дети участников СВО в приоритетном порядке
  и при наличии мест переводятся в детские сады и школы рядом с домом"
- `department` (golden) = "Единый центр поддержки участников СВО и их
  семей"
- `Ссылка на НПА` = `docs.cntd.ru/document/1300860766` (совпадает с уже
  используемым источником)

Прямой повторный фетч документа подтвердил текст п.1.2: "Предоставление
внеочередного права на перевод ребенка в другую наиболее приближенную к
месту жительства семьи государственную образовательную организацию,
предоставляющую общее образование... либо муниципальную образовательную
организацию, предоставляющую общее образование в городе Москве." — это
СЕМАНТИЧЕСКИ соответствует golden `measureTerms`, но не дословно
(юридическая формулировка НПА про "перевод ребенка... в организацию,
предоставляющую общее образование" vs golden разговорное "переводятся в
детские сады и школы рядом с домом") — тот же паттерн non-literal match,
что и в 77_svo_6/7 (terms не совпадёт по scorer'у из-за отсутствия
лемматизации/低 Jaccard, известное ограничение, не eval-change).
`categoryContractor`/`categoryVolunteer`/`kidsOfMilitary`/
`categoryMobilized` — та же логика и то же системное ограничение
источника, что и в предыдущих 3 сво-карточках (единая преамбула ст.1
указа на всю статью; "мобилизован" в документе не встречается ни разу —
подтверждено ещё в iteration_20260802_145212 полнотекстовым поиском).
`department` снова ожидается `None` — "Единый центр поддержки участников
СВО и их семей" в тексте указа не встречается (проверено для всех 4
предыдущих сво-карточек этой серии).

Ожидаемый эффект: Match сво 4→5 (общий 25→26), Recall общий 0.500→0.520.
Precision/AvgQS/PerfectRate — как обычно в этой серии, композиционный
эффект знаменателя возможен (новая карточка сама по себе не деградирует).

## Изменение

- Новая функция `extract_svo_kindergarten_transfer_card` в
  `revision_agent/pipeline.py` (п.1.2 указа), по образцу трёх соседних
  extract_svo_* функций.
- Пятая ветка диспатча в `run_svo_seed` по `measureName.startswith(...)`.
- Новый seed в `data/measures_registry.json` → `сво` (регион "Москва",
  measureName "Перевод в детский сад или школу рядом с домом", тот же
  npaUrl).

## Как проверяю

```
pyenv exec python3 scripts/run_pipeline_demo.py
pyenv exec python3 scripts/score_against_golden.py --agent-export data/output/agent_cards_export.json --note "B003: до/после добавления 77_svo_8"
```

Сравниваю Recall/Precision/AvgQS/PerfectRate до и после; проверяю, что
field_errors по 25 существующим карточкам не изменились (не регрессия).
