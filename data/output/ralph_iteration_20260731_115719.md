# Ralph iteration — 2026-07-31 11:57 — B004 (попытка), без коммита

## Задача

B004 — извлечь реальную карточку для мер `77_vbd_11..16` (льготы по
транспортному/земельному налогу), теперь когда `cntd.ru` фетчится через
прокси (см. предыдущий инфраструктурный коммит).

## Baseline (tuning_log.jsonl, последняя запись)

Average QS=0.333, Perfect Rate=0.000, Recall=0.020 (1/50), Precision=1.000
(из iteration_1 — с момента инфраструктурного коммита ничего не менялось,
подтверждено повторным прогоном без записи в лог).

## Гипотеза

Обе группы мер (77_vbd_11/12/13 — транспортный налог; 77_vbd_14/15/16 —
земельный налог) ссылаются на один и тот же базовый закон каждая
(документ 3691928 и 3656284 соответственно), с тремя категориями
получателей (Военнослужащие / Обслуживающие воинские части / Работники в
зоне боевых действий) — три отдельные карточки на общий текст условий.
Ожидал найти в тексте закона статью про льготы, перечисляющую эти
категории, и написать эвристику извлечения condition/department текста
+ (если категории явно перечислены в самом законе, а не выдуманы) —
categoryOfVeteran по каждой карточке, без копирования значений из
эталона.

## Что сделано и что не получилось

Зафетчил оба документа полностью через `revision_agent.pipeline.fetch_text(use_proxy=True)`:
- документ 3691928 (транспортный налог, 58681 симв. после unescape)
- документ 3656284 (земельный налог, 75864 симв.)

Искал по обоим текстам: `"боевых действий"`, `"ветеран"`,
`"Обслуживающие воинские части"`, `"Работники в зоне"`, `"Статья 4"`,
суммы `"1 000 000"`/`"1 млн"` — **ни одно совпадение не найдено ни в
одном из двух документов**. Тексты содержат только определения объекта
налогообложения и таблицы ставок — статьи со льготами в consolidated-
тексте, который отдаёт cntd.ru по этим двум ID, судя по всему, просто
нет.

## Вывод

Оба НПА-ссылки в эталоне (`Пункт НПА1` пусто для всех 6 мер) указывают,
по всей видимости, только на **базовый** закон о налоге, а не на
конкретную поправку/статью, которая реально вводит льготу ветеранам —
это ровно та ситуация, которую описывает `Обработка НПА.docx` (п.3.1):
льгота "установлена отдельным нормативным документом", и агент должен
идти по отсылке, а не считать базовый закон исчерпывающим источником.
Наскоро найти этот второй документ (например, через полнотекстовый
поиск по consultant.ru/поиск Яндекса, как в `auto/scripts/search_yandex.py`)
в рамках этой итерации не успел — это отдельная, более трудоёмкая
задача поиска по отсылкам, а не эвристика парсинга одной уже открытой
страницы.

**Код не менял, коммитить нечего** — согласно RALPH_PROMPT.md, "выглядит
похоже на прогресс" не считается результатом без реального измеримого
изменения; фабриковать карточку с невалидными полями, чтобы Match
подрос, было бы имитацией, а не извлечением.

## Решение

Не blocked (это не 3 провала подряд на одной и той же гипотезе — только
первая честная попытка с конкретным новым знанием: где искать не нужно,
и что нужно искать по отсылкам, а не в базовом законе). Backlog B004
обновлён с этой историей и уточнённой гипотезой для следующей попытки.
