# Ralph iteration 2026-08-11 23:38:55

## Задача

L002 — LLM-экстрактор: интеграция в pipeline + batch прогон.

## Baseline

- L001 done: `extract_measure_via_llm` (llm_extract_v2.py) больше не
  проваливается в fully-null на $ref-полях (проверено unit-level на
  77_vbd_4: 3/5 полей заполнены).
- `scripts/run_pipeline_demo.py` до этой итерации знает только про
  regex-раннеры (`run_vbd_seed`/`run_svo_seed`/`run_disability_seed`
  из `revision_agent/pipeline.py`) — нет пути через LLM.
- `agent/pipeline_mode.py` уже существует и умеет fetch+extract, но
  импортирует `revision_agent.llm_extract` (v1, БЕЗ фикса L001 —
  `extract_via_llm` там не имеет ветки `isinstance(entry, str)`).
- `score_against_golden.py` не запускался ни разу в LLM-эру — нет
  валидного baseline-числа для LLM-карточек.
- Реестр: вбд=6, сво=16, инвалиды=21 seed'ов (43 всего).

## Гипотеза

1. Добавление `--llm-mode` в `run_pipeline_demo.py`, который для каждого
   seed'а: fetch (переиспользуя `fetch_source` из `agent/pipeline_mode.py`)
   → `extract_measure_via_llm` (llm_extract_v2, с L001-фиксом) →
   `write_agent_export`, даст рабочий LLM-batch-путь.
2. Полный batch-прогон по всем 43 seed'ам с generic LLM-экстрактором
   даст Recall > 0 (нижняя планка успеха L002) — вероятно ниже regex-эры
   (0.86) на первой попытке без тюнинга промпта (это L003), но заведомо
   ненулевой, так как экстрактор уже проверен e2e на одном кейсе.

## План проверки

```bash
python3 scripts/run_pipeline_demo.py --llm-mode
python3 scripts/score_against_golden.py \
    --agent-export data/output/agent_cards_export.json \
    --note "L002: LLM batch первый прогон"
```

Критерий успеха (по backlog): Recall > 0 и измеримые AvgQS/PerfectRate.

## Результат

Реализован `--llm-mode` в `scripts/run_pipeline_demo.py` (функция
`run_llm_mode`): переиспользует `agent.pipeline_mode.fetch_source` для
загрузки и `revision_agent.llm_extract_v2.extract_measure_via_llm`
(с L001-фиксом) для экстракции, пишет через существующий
`write_agent_export`.

Batch-прогон по всем 43 seed'ам (в фоне, ~40+ LLM-вызовов, заняло
несколько десятков минут) прошёл без падений. `score_against_golden.py`:

```
Recall:    0.860  Precision: 1.000
Average QS:   0.248  Perfect Rate: 0.000  (43 карточки)

[вбд]      Recall=0.500 Precision=1.000 AvgQS=0.311 (scored=6)
[сво]      Recall=1.000 Precision=1.000 AvgQS=0.263 (scored=16)
[инвалиды] Recall=0.955 Precision=1.000 AvgQS=0.219 (scored=21)
```

7 miss_agent — все из-за ошибки *загрузки* источника (в основном
mos.ru, известная ГОСТ-TLS проблема, не связана с LLM-экстракцией).

Критерий успеха L002 выполнен: Recall > 0 (0.860), метрики AvgQS/
PerfectRate измеримы. AvgQS низкий по сравнению с regex-эрой (0.61) —
ожидаемо для первого прогона без тюнинга промпта под конкретные поля;
разбор и тюнинг — задача L003 (уже в backlog, приоритет high).

Побочное наблюдение: почти 100% ошибок по "category"-полям во всех
трёх ЖС — вероятно частично пересекается с L006 (boolean-коэрсия ломает
текстовые category-поля вбд), но также затрагивает сво/инвалиды, где
category и должна быть булевой — нужен отдельный разбор в L003.

## Вердикт: commit

Backlog: L002 → `done`. tuning_log.jsonl дополнен automatически
scorer'ом (append-only, не редактировался вручную). PROGRAM.md
дополнен записью об итерации.
